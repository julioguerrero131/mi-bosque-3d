using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FourthVerificationDialog : Desafio
{
    public GameObject challenge;

    /*void OnTriggerEnter(Collider obj)
    {
        if (obj.gameObject.tag == "Player")
        {
            if (Pick.recogidos == 3)
            {
                disappear = true;
                imageCorrecto.SetActive(true);
                GameObject.FindGameObjectWithTag("Bag").GetComponent<Inventory>().TestRemoveF(1);
                GameObject.FindGameObjectWithTag("Bag").GetComponent<Inventory>().TestRemoveF(1);
                GameObject.FindGameObjectWithTag("Bag").GetComponent<Inventory>().TestRemoveF(2);
                IntroductionDialog(0);
                
            }
            else
            {
                IntroductionDialog(1);
            }

        }
    }*/
}
